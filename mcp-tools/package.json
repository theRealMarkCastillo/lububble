{
  "name": "@lububble/mcp-tools",
  "version": "0.1.0",
  "type": "module",
  "description": "MCP tool pack for Lububble: workspace file ops, Docker compose runner, health checks",
  "main": "dist/index.js",
  "bin": {
    "lububble-mcp-tools": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p . && cp -R src/fixtures dist/fixtures",
    "test": "node dist/test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "yaml": "^2.9.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
